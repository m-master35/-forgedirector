window.FORGE_DIRECTOR_CONFIG = {
  // Filled with the deployed AWS API Gateway endpoint after the Bedrock stack is deployed.
  // Leave empty to keep the deterministic hackathon simulation active.
  apiUrl: '',
};
